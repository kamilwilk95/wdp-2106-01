/* selectors */
export const getBrands = ({ brands }) => brands;

/* reducer */
export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
